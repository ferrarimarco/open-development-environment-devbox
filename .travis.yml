---
language: shell

sudo: required

services:
  - docker

jobs:
  include:
    - stage: lint shell scripts
      script: docker run --rm -it --name df-shellcheck -v $(pwd):/usr/src:ro ferrarimarco/shellcheck

stages:
  - lint shell scripts
